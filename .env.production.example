# Build de produção (Cloudflare Workers). Configurar como "Variable" (não
# secret) no GitHub Actions (Settings → Environments/Variables) com este nome:
# VITE_API_URL

VITE_API_URL=https://api.SEUDOMINIO.com
